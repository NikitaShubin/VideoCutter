Django>=4.2,<5.0
django-cors-headers>=4.0
numpy>=1.24
opencv-python>=4.8
av>=10
# Прод-сервер: gthread, строго workers=1 (GOP-кэш живёт в памяти процесса —
# второй воркер разрезал бы кэш, RAM и автотюн; масштабирование — потоками).
gunicorn>=21,<24